block('page-cleaned').content()(function () {
    return [
        {
            block: 'b-text',
            elem: 'h1',
            content: 'Project is cleaned up.'
        },
        {
            block: 'b-project-menu',
            projectId: this.route.parameters.projectId
        }
    ];
});

block('page-cleaned').elem('title').content()('Project is cleaned up.');
