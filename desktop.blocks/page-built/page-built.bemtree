block('page-built').content()(function () {
    return [
        {
            block: 'b-text',
            elem: 'h1',
            content: 'Project is built.'
        },
        {
            block: 'b-project-menu',
            projectId: this.route.parameters.projectId
        }
    ];
});

block('page-built').elem('title').content()('Project is built.');
